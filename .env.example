# 실행 환경: local | dev | prod
NODE_ENV=local

# 서버 포트
PORT=3000

# DB 접속 정보 (로컬은 docker-compose 기준)
# 접속 문자열 대신 쪼개서 쓰는 이유는 src/config/env.ts 주석 참고
DB_HOST=localhost
DB_PORT=5434
DB_USER=postgres
DB_PASSWORD=postgres
DB_NAME=flyer_g
DB_SSL=false
